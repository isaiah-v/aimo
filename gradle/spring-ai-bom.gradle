// Apply the dependency-management plugin and import the Spring AI BOM using the
// centralized `springAiVersion` property defined in the root build script.
apply plugin: 'io.spring.dependency-management'

dependencyManagement {
    imports {
        mavenBom "org.springframework.ai:spring-ai-bom:${project.property('springAiVersion')}"
    }
}

